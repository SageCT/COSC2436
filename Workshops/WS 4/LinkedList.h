
template <typename T>
class LinkedList
{
    void LinkedList::insert(int index, const T &item)
    {
        if (index = 0)
        {
            next = nullptr;
            value = T;
        }
    }
};